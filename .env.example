# No environment variables are required to run Webzyra as delivered.
# This file is a placeholder for future use (e.g. if a backend, form
# handler, or payment gateway is added later). Never commit a real
# .env or .env.local file with secrets to GitHub.
